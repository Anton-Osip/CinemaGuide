import {s} from "./Search_styles"

export const Search: React.FC = () => {
    return (
        <s.Form>
            <s.Input placeholder='Поиск' type = "text"/>
            <button></button>
        </s.Form>
    )
}